export default `# HelloWorld Lookup Service Documentation

## Overview
The **HelloWorld Lookup Service** (service ID: \`ls_helloworld\`) lets clients search the on‑chain *Hello‑World* messages that were indexed by the **HelloWorld Topic Manager**. Each record represents a Pay‑to‑Push‑Drop output whose single field is a UTF‑8 message of at least two characters.
`
